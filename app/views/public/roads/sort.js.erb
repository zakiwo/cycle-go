$('.road-lists').html("<%= escape_javascript(render partial: 'shared/roads/roads',locals:{roads: @roads, title: 'Cycling Roads'}) %>");
$('.road-map').html("<%= escape_javascript(render partial: 'shared/roads/map',locals:{roads: @roads }) %>");